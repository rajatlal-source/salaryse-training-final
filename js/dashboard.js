const empId=localStorage.getItem('empId');document.getElementById('empName').innerText=empId||'Learner';function startDay(day){localStorage.setItem('currentDay',day);window.location.href='module.html';}const lb=JSON.parse(localStorage.getItem('leaderboard')||'[]');const ul=document.getElementById('leaderboard');lb.sort((a,b)=>b.xp-a.xp).forEach(p=>{const li=document.createElement('li');li.textContent=p.id+': '+p.xp+' XP';ul.appendChild(li);});